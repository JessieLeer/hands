import Vue from 'vue'

// initial state
const state = {
	type: ''
}

// getters
const getters = {
}

// actions
const actions = {
	
}

// mutations
const mutations = {
	handleType(state,value) {
		state.type = value
	}
}

export default {
  state,
  getters,
  actions,
  mutations
}